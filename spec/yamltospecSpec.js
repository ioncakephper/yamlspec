const yamlspec = require('../index');


describe('yamlspec.loadFile', () => {

    it('should return empty object when sourceFilename does not exist', () => {
        //
        // @todo should return empty object when sourceFilename does not exist
        //
        expect(0).toEqual(1);
    })

    it('should return empty object when sourceFilename is not a string', () => {
        //
        // @todo should return empty object when sourceFilename is not a string
        //
        expect(0).toEqual(1);
    })

    it('should return an object containing \'items\' key and array is values', () => {
        //
        // @todo should return an object containing \'items\' key and array is values
        //
        expect(0).toEqual(1);
    })

    it('should return object with \'items\' key when sourceFile contains \'items\'', () => {
        //
        // @todo should return object with \'items\' key when sourceFile contains \'items\'
        //
        expect(0).toEqual(1);
    })

    it('items should be an array of objects', () => {
        //
        // @todo items should be an array of objects
        //
        expect(0).toEqual(1);
    })
})
